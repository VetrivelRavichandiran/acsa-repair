[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "acsa-repair"
version = "0.1.0"
description = "Runtime state-delta feedback prototype for LLM-based program repair"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["acsa_repair"]
